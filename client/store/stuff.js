// ACTION TYPES

// INITIAL STATE
const defaultStuff = {}

// ACTION CREATORS

// THUNK CREATORS

// REDUCER
export default (state = defaultStuff, action) => {
  switch (action.type) {
    default:
      return state
  }
}
